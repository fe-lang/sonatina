target = "evm-ethereum-london"

func private %abort_chain(v0.i32) {
    block0:
        v1.i32 = add v0 1.i32;
        v2.i32 = mul v1 2.i32;
        v3.i32 = add v2 v1;
        evm_revert v2 v3;
}

func public %caller(v0.i32) {
    block0:
        call %abort_chain v0;
        return;
}
