target = "evm-ethereum-london"

# Test: x <= x should be simplified to true (1)
# sameln: func public %le_self(v0.i32) -> i1 {
# check:         return 1.i1;
func public %le_self(v0.i32) -> i1 {
    block0:
        v1.i1 = le v0 v0;
        return v1;
}