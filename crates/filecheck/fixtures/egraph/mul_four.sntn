target = "evm-ethereum-london"

# Test: x * 4 should be strength-reduced to x << 2
# We test via 0 * 4 which should become 0 << 2 = 0
# sameln: func public %mul_four() -> i32 {
# check:         return 0.i32;
func public %mul_four() -> i32 {
    block0:
        v0.i32 = mul 0.i32 4.i32;
        return v0;
}