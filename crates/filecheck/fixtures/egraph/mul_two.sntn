target = "evm-ethereum-london"

# Test: x * 2 should be strength-reduced to x << 1
# We test via 0 * 2 which should become 0 << 1 = 0
# sameln: func public %mul_two() -> i32 {
# check:         return 0.i32;
func public %mul_two() -> i32 {
    block0:
        v0.i32 = mul 0.i32 2.i32;
        return v0;
}