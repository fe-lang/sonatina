target = "evm-ethereum-london"

# Test: x >> 0 (arithmetic) should be simplified to x
# sameln: func public %sar_zero(v0.i32) -> i32 {
# check:         return v0;
func public %sar_zero(v0.i32) -> i32 {
    block0:
        v1.i32 = sar 0.i32 v0;
        return v1;
}