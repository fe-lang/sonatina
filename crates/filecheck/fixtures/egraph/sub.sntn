target = "evm-ethereum-london"

# Test: x - 0 = x
# sameln: func public %sub_zero(v0.i32) -> i32 {
# check:         return v0;
func public %sub_zero(v0.i32) -> i32 {
    block0:
        v1.i32 = sub v0 0.i32;
        return v1;
}

# Test: x - x = 0
# sameln: func public %sub_self(v0.i32) -> i32 {
# check:         return 0.i32;
func public %sub_self(v0.i32) -> i32 {
    block0:
        v1.i32 = sub v0 v0;
        return v1;
}