#! Simple constant folding without branching.

# sameln: func %simple_fold() -> i256:
# nextln:     block0:
# nextln:         return -1.i256;
func %simple_fold() -> i256:
    block0:
        v0.i256 = not 0.i256;
        return v0;
